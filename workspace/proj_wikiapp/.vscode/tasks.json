{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "deploy",
            "type": "shell",
            "command": "databricks bundle deploy -t dev && databricks bundle run wiki_app -t dev",
            "group": "build"
        }
    ]
}