[project]
name = "sample-job"
version = "0.1.0"
description = "Sample Databricks job with config management"
requires-python = ">=3.12.3"
dependencies = []

[project.optional-dependencies]
vsc = [
    "ipykernel>=7.2.0",
]
